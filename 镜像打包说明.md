1. 项目已经创建docker-compose 文件，使用该文件进行打包
请勿改动docker目录下的docker file前后端相关文件！！

2. 镜像打包命令：
docker-compose --build --no-cache   删除缓存重新打包，自动生成前后端镜像
docker-compose up  启动前后端容器，
docker-compose down  停止前后端容器，删除前后端容器，

后端API接口查看：
http://localhost:8000/docs
http://localhost:8000/redoc  可导出openapi.json文件，导入到postman等工具查看接口

3. 导出镜像到本地
docker save -o data_preprocess_backend.tar data_preprocess_backend
docker save -o data_preprocess_frontend.tar data_preprocess_frontend

4. 导入镜像到服务器
docker load -i data_preprocess_backend.tar
docker load -i data_preprocess_frontend.tar
同步将docker-compose.yml \ .env.docker文件导入到服务器
注：服务器上无法使用后端API网页查看接口


5.导入后启动容器
在docker-compose.yml \ .env.docker文件所在目录执行命令：docker-compose up 启动前后端容器
.env.docker文件中配置了数据库连接信息，需要根据实际情况修改
docker-compose 文件中配置了后端服务地址，根据实际情况可修改为宿主机IP


6. 启动前后端联调
修改代码后，重新打包镜像导入服务器






